static const uint3 gl_WorkGroupSize = uint3(1u, 1u, 1u);

RWByteAddressBuffer root_buffer_0 : register(u0);

static uint3 gl_GlobalInvocationID;
struct SPIRV_Cross_Input
{
    uint3 gl_GlobalInvocationID : SV_DispatchThreadID;
};

void comp_main()
{
    if (gl_GlobalInvocationID.x == 0u)
    {
        root_buffer_0.Store(30408704, uint(0));
        root_buffer_0.Store(30408708, uint(0));
        root_buffer_0.Store(30408712, uint(25000));
        root_buffer_0.Store(30408716, uint(25000));
        root_buffer_0.Store(30408720, uint(25000));
        root_buffer_0.Store(30408724, uint(100000));
        root_buffer_0.Store(30408728, uint(100000));
        root_buffer_0.Store(30408732, uint(100000));
        root_buffer_0.Store(30408736, uint(140000));
    }
}

[numthreads(1, 1, 1)]
void main(SPIRV_Cross_Input stage_input)
{
    gl_GlobalInvocationID = stage_input.gl_GlobalInvocationID;
    comp_main();
}
