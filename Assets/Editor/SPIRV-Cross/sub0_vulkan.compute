static const uint3 gl_WorkGroupSize = uint3(128u, 1u, 1u);

RWByteAddressBuffer root_buffer_0 : register(u0);

static uint3 gl_GlobalInvocationID;
struct SPIRV_Cross_Input
{
    uint3 gl_GlobalInvocationID : SV_DispatchThreadID;
};

void comp_main()
{
    int _21 = int(gl_GlobalInvocationID.x);
    for (int ii = _21; ii < 262144; )
    {
        uint _56 = uint(((ii & 63) + (((ii >> 6) & 63) << 6)) + (((ii >> 12) & 63) << 12));
        uint _58 = _56 * 12u;
        root_buffer_0.Store(uint(int(6553600u + _58) >> int(2u)) * 4 + 0, uint(0));
        root_buffer_0.Store(uint(int(_58 + 6553604u) >> int(2u)) * 4 + 0, uint(0));
        root_buffer_0.Store(uint(int(_58 + 6553608u) >> int(2u)) * 4 + 0, uint(0));
        root_buffer_0.Store(uint(int(9699328u + (_56 * 4u)) >> int(2u)) * 4 + 0, uint(0));
        ii += 262144;
        continue;
    }
}

[numthreads(128, 1, 1)]
void main(SPIRV_Cross_Input stage_input)
{
    gl_GlobalInvocationID = stage_input.gl_GlobalInvocationID;
    comp_main();
}
